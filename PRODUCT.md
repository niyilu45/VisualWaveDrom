# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Product Name

VisualWaveDrom

## Primary Users

- 通信算法工程师。
- 需要在本地创建、整理、检查和复用波形数据的工程人员。
- 需要把大规模采样数据提炼成便于分析、评审和文档展示的波形用例的用户。

## Core Purpose

VisualWaveDrom 用于编辑、归档和分析波形，并把大数据波形简化成可展示、可复用的工程用例。

## Positioning

VisualWaveDrom 是一个本地优先的工程波形工作台。它在同一套界面中提供 WaveDrom 波形编辑、层级化波形库管理、大数据示波器视图、数据导入和工程文档引用能力，重点服务于通信算法分析与用例整理。

## Operating Context

- 在 Windows 和 Linux 桌面浏览器中使用。
- 支持本地服务模式和直接打开 HTML 的模式。
- 波形库需要便于离线保存、复制、迁移和交付。
- 既要处理适合文档展示的小型波形，也要处理列数很大的采样数据。
- 当前兼容目标包括 Chrome、Edge 和 Firefox 116 及以上版本。

## Existing Product Commitments

- 保留当前已经存在的全部功能、操作逻辑和数据行为。
- 保留 WaveDrom JSON 的显示、编辑和兼容能力。
- 保留 SQLite 单文件波形库及现有波形库数据的兼容性。
- 保留多波形、层级目录、分组、连接线、说明文字、撤销与重做、Vim 操作、数据导入、截图与单图编辑等现有工作流。
- 保留大数据模式和示波器模式，持续关注大规模波形的加载、滚动、编辑和定位性能。
- 保留本地优先、离线可用和跨 Windows/Linux 迁移的能力。

## Interface Optimization Boundary

界面优化可以调整：

- 信息层级、布局、间距、视觉样式和内容密度。
- 控件的可发现性、状态反馈、错误提示和可读性。
- 响应式行为、键盘焦点、无障碍表现和不改变结果的渲染性能。

未经用户明确确认，界面优化不得：

- 删除、新增、改名或重新定义任何功能。
- 改变数据结构、存储格式、导入导出结果或已有波形库内容。
- 改变快捷键、默认值、编辑规则、操作顺序或业务逻辑。
- 破坏现有浏览器、操作系统和波形数据兼容性。

## Product Principles

1. **功能完整优先**：视觉优化必须建立在现有功能完全保留的前提上。
2. **数据准确可靠**：波形、文本、连接关系和目录归属在任何编辑操作后都必须保持正确。
3. **高效工程操作**：减少等待、重复操作和无意义跳转，让高频编辑保持直接。
4. **小图与大数据并重**：同一产品既能精细编辑展示用例，也能流畅浏览和简化大规模数据。
5. **本地可控且易迁移**：用户拥有完整数据，波形库可以离线使用并在不同电脑之间稳定迁移。

## Evidence on Hand

- 当前 `ReadMe.md` 中记录的功能、运行方式、浏览器兼容范围和使用流程。
- 现有 VisualWaveDrom 界面、源代码、示例波形库、导入预设及操作演示素材。
- 用户已确认的目标用户、核心价值和“只优化界面、不改变功能”的约束。
